apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: fn-get-nationality-vs
  namespace: fission
spec:
  hosts:
    - dev.api.siga.me
  gateways:
    - develop/dev-istio-gateway
  http:
  - match:
    - uri:
        prefix: /router/enum/nationality
    rewrite:
      uri: /enum/nationality
    route:
    - destination:
        host: router.fission.svc.cluster.local
        port:
          number: 80